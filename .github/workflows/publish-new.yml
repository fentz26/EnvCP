name: Publish to npm, PyPI, Docker Hub, and MCP Registry
on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      npm_tag:
        description: 'npm dist-tag'
        required: false
        default: 'latest'

permissions:
  contents: write

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test"
